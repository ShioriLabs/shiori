name: deploy

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Server target to deploy'
        required: true

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
    - name: 'Hello World'
      run: |
        echo 'Hello to ${{ github.event.inputs.target }}'
